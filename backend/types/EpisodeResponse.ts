type EpisodeResponse = {
    episodeId: string,
    url: string,
    title: string,
    episodeNumber: number,
    season: number,
    timestamp: number,
    categories: string[]
}

export default EpisodeResponse;